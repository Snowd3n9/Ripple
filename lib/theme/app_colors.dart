import 'package:flutter/material.dart';

class RippleColors {
  const RippleColors._();

  static const Color background = Color(0xFF050A16);

  static const Color sidebar = Color(0xFF0D1526);

  static const Color surface = Color(0xFF111827);

  static const Color card = Color(0xFF171F31);

  static const Color accent = Color(0xFF1E88FF);

  static const Color textPrimary = Colors.white;

  static const Color textSecondary = Color(0xFF9AA6BF);
}
